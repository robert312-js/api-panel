"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var src_1 = require("../src");
describe('codec', function () {
    var encoded = src_1.encode(src_1.PacketType.SERVERDATA_AUTH, 22, 'password');
    it('decode', function () {
        var decoded = src_1.decode(encoded);
        expect(decoded.id).toBe(22);
        expect(decoded.type).toBe(src_1.PacketType.SERVERDATA_AUTH);
        expect(decoded.body).toBe('password');
        expect(decoded.size).toBe(18);
    });
});
